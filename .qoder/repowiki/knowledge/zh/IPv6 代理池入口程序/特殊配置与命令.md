安装需 root 执行 `scripts/install.sh`，它会安装 `golang-go git ndppd curl`、编译二进制至 `/opt/ipv6-proxy/ipv6-proxy`、写入 `/etc/systemd/system/ipv6-proxy.service` 并启用；运行前需在宿主机配置本地路由（`ip route add local <your-prefix> dev <iface>`）及 `ndppd` 以响应邻居发现。